{
  "name": "poo2-tp3-gassino-idoldi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: Shoul run test with command npm run testpush\" && exit 1",
    "testpush": "git add . && git commit -m \"TDD iteration\" && git push origin master parte2 && jest"
    },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.2.1"
  }
}
